---
title: "Hello, world"
date: "2026-05-10"
excerpt: "A first post — just to prove the blog works."
---

This is a sample post. The blog reads Markdown files from `content/posts/` at
build time, parses the frontmatter with `gray-matter`, and renders them as
static pages.

To add a new post, drop a new `.md` file next to this one with frontmatter for
`title`, `date`, and `excerpt`. That's it.

## A heading

Some prose. The styling is intentionally minimal so writing comes first.

- A list item
- Another list item
- One more

```ts
function hello(name: string) {
  return `hello, ${name}`;
}
```

> A blockquote, for emphasis.
